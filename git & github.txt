Git : Git is a distributed version control system. we need to download and install git
  steps:
-- Go to following link
  https://git-scm.com/downloads --> windows --->64 bit

  Install by agrreing and install

1. go to terminal --> new terminal -->cmd
git init
git add .
git commit -m "message"

but for first time, git config --global user.email "emailid"
git config --global user.name "name"

  github: git hub is a website to store,manage,deploy codes
  goto github.com and create an account and repostory
  open folder in vscode, open terminal and write following
  git remote remove origin (if it is already existed)
  git remote add origin link/path
  git push -u origin main 



  2. pull your repository